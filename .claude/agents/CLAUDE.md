@README.md

Agent files are the CI entry point for digest generation. Each agent file references its corresponding skill in `.claude/skills/` and adds CI-specific adaptations for headless execution in GitHub Actions.
